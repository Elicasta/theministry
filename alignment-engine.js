(()=>{'use strict';const C=window.KP_ALIGNMENT;if(!C)return;const {room:ROOM,channel:CHANNEL,type:TYPE,series:SERIES,lesson:LESSON,slides:SLIDES,verses:VERSES,polls:POLLS}=C;const view=(new URLSearchParams(location.search).get('view')||'remote').toLowerCase();const STUDENT_URL=location.origin+'/alignment/',GUIDE_URL='https://apostolicguide.com/';let state={room:ROOM,started:false,slide:0,overlay:null,black:false,activePoll:null,activeQuestion:null,startedAt:null,session:'kp4_'+Date.now().toString(36),seq:0,ts:Date.now()},bc=null,sbUrl='',sbKey='',sbClient=null,sbChannel=null,writeChain=Promise.resolve(),questions=[],votes=[];function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function titleOf(s){return s?.title||s?.ref||s?.text||s?.kicker||'Slide'}function qr(url){return'https://api.qrserver.com/v1/create-qr-code/?size=260x260&data='+encodeURIComponent(url)}function pollBy(id){return POLLS.find(p=>p.id===id)}function voteCounts(p){const c=Object.fromEntries((p?.options||[]).map(o=>[o,0]));votes.filter(v=>v.poll_id===p?.id).forEach(v=>{if(v.answer in c)c[v.answer]++});return c}function totalCounts(c){return Object.values(c).reduce((a,b)=>a+b,0)}function clearModes(){state.overlay=null;state.activePoll=null;state.activeQuestion=null}function renderSlide(s,withQr=true){if(!s)return'';const k=s.kicker?`<div class="p-kicker">${esc(s.kicker)}</div>`:'',sub=s.sub?`<div class="p-sub">${esc(s.sub)}</div>`:'',ref=s.ref?`<div class="p-ref">${esc(s.ref)}</div>`:'';if(s.type==='cover')return`<div class="cover">${k}<div class="p-title">${esc(s.title)}</div><div class="rule"></div>${sub}${ref}${withQr?`<div class="qr-row"><div class="qr"><img src="${qr(STUDENT_URL)}"><strong>Join Lesson</strong><span>theministry.vercel.app/alignment</span></div><div class="qr"><img src="${qr(GUIDE_URL)}"><strong>Apostolic Guide</strong><span>apostolicguide.com</span></div></div>`:''}</div>`;if(s.type==='verse')return`${k}${ref}<div class="rule"></div><div class="p-verse">${esc(s.text)}</div>${sub}`;if(s.type==='statement')return`${k}<div class="statement">${esc(s.title)}</div>${sub}${ref}`;if(s.type==='prompt')return`${k}<div class="p-title med">${esc(s.title)}</div>${sub}${ref}`;if(s.type==='points'||s.type==='illustration')return`${k}<div class="p-title med">${esc(s.title)}</div><ul class="points">${(s.points||[]).map(p=>`<li>${esc(p)}</li>`).join('')}</ul>${sub}${ref}`;if(s.type==='contrast')return`${k}<div class="contrast"><div class="contrast-box"><div class="contrast-text">${esc(s.left)}</div></div><div class="contrast-box right"><div class="contrast-text">${esc(s.right)}</div></div></div>${sub}`;if(s.type==='steps')return`${k}<div class="p-title med">${esc(s.title)}</div><div class="steps">${(s.steps||[]).map((x,i)=>`<div class="step"><span class="step-n">${i+1}</span><span class="step-t">${esc(x)}</span></div>`).join('')}</div>${sub}`;if(s.type==='check')return`${k}<div class="check">${(s.items||[]).map(x=>`<div class="check-item"><div class="check-l">${esc(x[0])}</div><div class="check-q">${esc(x[1])}</div></div>`).join('')}</div>`;if(s.type==='levels')return`${k}<div class="levels">${(s.levels||[]).map(x=>`<div class="level"><div class="level-n">${esc(x[0])}</div><div class="level-t">${esc(x[1])}</div><div class="level-q">${esc(x[2])}</div></div>`).join('')}</div>`;return`${k}<div class="p-title med">${esc(titleOf(s))}</div>${sub}${ref}`}function takeoverHtml(){if(state.overlay)return`<div class="take-label">${esc(state.overlay.ref)}</div><div class="take-text">${esc(state.overlay.text)}</div>`;if(state.activeQuestion)return`<div class="take-label">Anonymous Question</div><div class="take-text">${esc(state.activeQuestion.text)}</div>`;if(state.activePoll){const p=pollBy(state.activePoll.id);if(!p)return'';if(state.activePoll.mode==='question')return`<div class="take-label">Live Poll · ${esc(p.cue)}</div><div class="p-title med">${esc(p.question)}</div><div class="p-sub">Vote from the student portal</div>`;const c=state.activePoll.results||voteCounts(p),tot=state.activePoll.total??totalCounts(c);return`<div class="take-label">Poll Results · ${esc(p.cue)} · ${tot} response${tot===1?'':'s'}</div><div class="p-title med">${esc(p.question)}</div><div class="poll-options">${p.options.map(o=>{const n=c[o]||0,pct=tot?Math.round(n/tot*100):0;return`<div class="poll-opt"><div class="poll-label">${esc(o)}</div><div class="poll-bar"><span style="width:${pct}%"></span></div><div class="poll-pct">${pct}%</div></div>`}).join('')}</div>`}return''}function lowerData(){if(state.overlay)return{title:state.overlay.ref,sub:state.overlay.text};if(state.activeQuestion)return{title:'Anonymous Question',sub:state.activeQuestion.text};if(state.activePoll){const p=pollBy(state.activePoll.id);return p?{title:p.question,sub:state.activePoll.mode==='results'?'Live results':'Vote now'}:null}if(!state.started)return null;const s=SLIDES[state.slide];return{title:titleOf(s),sub:s.sub||s.ref||s.kicker||''}}function renderProjector(){const inner=document.getElementById('p-inner'),take=document.getElementById('p-take'),black=document.getElementById('p-black');if(inner)inner.innerHTML=state.started?renderSlide(SLIDES[state.slide]):renderSlide(SLIDES[0]);const th=takeoverHtml();if(take){document.getElementById('p-take-inner').innerHTML=th;take.classList.toggle('hidden',!th)}if(black)black.classList.toggle('hidden',!state.black)}function renderOutputs(){if(view==='projector')renderProjector();if(view==='obsslides'){const el=document.getElementById('obs-slide-inner');if(el)el.innerHTML=state.started?renderSlide(SLIDES[state.slide],false):renderSlide(SLIDES[0],false)}if(view==='obslowerthirds'){const d=lowerData(),box=document.getElementById('lower-box');if(box)box.classList.toggle('hidden',!d);if(d){document.getElementById('lower-title').textContent=d.title;document.getElementById('lower-sub').textContent=d.sub}}if(view==='scriptures'){const c=document.getElementById('scripture-content');if(c)c.innerHTML=state.overlay?`<div class="scripture-ref">${esc(state.overlay.ref)}</div><div class="scripture-text">${esc(state.overlay.text)}</div>`:'<div class="p-kicker">Kingdom Principles · Scripture Output</div><div class="p-title">WAITING FOR SCRIPTURE</div><div class="p-sub">Push a verse from the remote</div>'}if(view==='confidence')renderConfidence()}function renderConfidence(){const s=SLIDES[state.slide]||SLIDES[0],next=state.started&&state.slide<SLIDES.length-1?SLIDES[state.slide+1]:null,k=document.getElementById('conf-k'),t=document.getElementById('conf-title'),r=document.getElementById('conf-ref');if(!k||!t||!r)return;if(state.overlay){k.textContent='Active Scripture';t.style.fontFamily='var(--fp)';t.style.textTransform='none';t.style.fontSize='clamp(1.7rem,2.6vw,3.2rem)';t.textContent=state.overlay.text;r.textContent=state.overlay.ref}else if(state.activeQuestion){k.textContent='Anonymous Question';t.style.fontFamily='var(--fp)';t.style.textTransform='none';t.style.fontSize='clamp(1.8rem,3vw,3.8rem)';t.textContent=state.activeQuestion.text;r.textContent='Live Q&A'}else if(state.activePoll){const p=pollBy(state.activePoll.id);k.textContent=state.activePoll.mode==='results'?'Poll Results':'Live Poll';t.style.fontFamily='var(--fd)';t.style.textTransform='uppercase';t.style.fontSize='clamp(2.5rem,4vw,5rem)';t.textContent=p?.question||'Poll';r.textContent=state.activePoll.mode==='results'?`${state.activePoll.total||0} responses`:'Voting open'}else{k.textContent='Current Slide';t.style.fontFamily='var(--fd)';t.style.textTransform='uppercase';t.style.fontSize='clamp(3.3rem,5.5vw,7rem)';t.textContent=state.started?titleOf(s):C.title;r.textContent=state.started?(s.ref||s.kicker||`Slide ${state.slide+1}`):C.primaryRef}document.getElementById('conf-next').textContent=next?titleOf(next):'End';document.getElementById('conf-notes').textContent=state.started?(s.notes||''):'Standby.'}function updateTimer(){if(view!=='confidence')return;const secs=state.startedAt?Math.max(0,Math.floor((Date.now()-state.startedAt)/1000)):0;document.getElementById('conf-time').textContent=String(Math.floor(secs/60)).padStart(2,'0')+':'+String(secs%60).padStart(2,'0')}function setCue(){if(!['remote','admin','mobile'].includes(view))return;const s=SLIDES[state.slide]||SLIDES[0];document.getElementById('cue-num').textContent=state.started?String(state.slide+1).padStart(2,'0'):'00';document.getElementById('cue-title').textContent=state.started?titleOf(s):'Standby';document.getElementById('cue-type').textContent=state.started?(s.type+' · Week 4'):'Week 4 · Alignment';document.getElementById('cue-notes').textContent=state.started?(s.notes||''):C.title+' · '+C.subtitle;document.getElementById('next-cue').innerHTML='<strong>Next</strong>'+esc(!state.started?titleOf(SLIDES[0]):state.slide<SLIDES.length-1?titleOf(SLIDES[state.slide+1]):'End');document.getElementById('next-btn').textContent=!state.started?'Start':state.slide<SLIDES.length-1?'Next →':'End';document.getElementById('black-btn').classList.toggle('on',state.black);document.querySelectorAll('.slide-item').forEach((el,i)=>el.classList.toggle('on',state.started&&i===state.slide));renderAudienceData()}function apply(next){if(!next||next.room!==ROOM)return;state={...state,...next};setCue();renderOutputs()}function payload(){return{...state,room:ROOM,ts:Date.now()}}function send(){state.seq=(state.seq||0)+1;state.ts=Date.now();const msg={type:TYPE,room:ROOM,state:payload()};try{if(!bc)bc=new BroadcastChannel(CHANNEL);bc.postMessage(msg)}catch{}sendNetwork(msg);setCue();renderOutputs()}function sendNetwork(msg){if(!sbUrl||!sbKey)return;writeChain=writeChain.catch(()=>{}).then(()=>fetch(sbUrl+'/rest/v1/sync_state',{method:'POST',headers:{'Content-Type':'application/json',apikey:sbKey,Authorization:'Bearer '+sbKey,Prefer:'resolution=merge-duplicates,return=minimal'},body:JSON.stringify({id:1,payload:JSON.stringify(msg),updated_at:new Date().toISOString()})}).then(r=>setStatus(r.ok,'Live sync')).catch(()=>setStatus(false,'Local only')))}function handle(msg){if(msg?.type!==TYPE||msg.room!==ROOM)return;apply(msg.state)}function startLocal(){try{bc=new BroadcastChannel(CHANNEL);bc.onmessage=e=>handle(e.data)}catch{}}function setStatus(ok,label){const d=document.getElementById('r-dot');if(!d)return;d.classList.toggle('live',ok);document.getElementById('r-sync').textContent=label}async function readLatest(){if(!sbUrl||!sbKey)return;try{const r=await fetch(sbUrl+'/rest/v1/sync_state?id=eq.1&select=payload',{headers:{apikey:sbKey,Authorization:'Bearer '+sbKey},cache:'no-store'}),rows=await r.json(),raw=rows?.[0]?.payload,msg=typeof raw==='string'?JSON.parse(raw):raw;if(msg?.type===TYPE&&msg.room===ROOM&&Date.now()-(msg.state?.ts||0)<43200000)handle(msg)}catch{}}async function initNetwork(){try{const r=await fetch('/api/config',{cache:'no-store'}),c=await r.json();sbUrl=c.supabaseUrl||'';sbKey=c.supabaseAnonKey||''}catch{}if(!sbUrl||!sbKey){setStatus(false,'Local only');return}await readLatest();const sc=document.createElement('script');sc.src='https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.min.js';sc.onload=()=>{try{sbClient=window.supabase.createClient(sbUrl,sbKey);sbChannel=sbClient.channel('kp-alignment-room').on('postgres_changes',{event:'*',schema:'public',table:'sync_state'},p=>{try{const raw=p.new?.payload,msg=typeof raw==='string'?JSON.parse(raw):raw;handle(msg)}catch{}}).subscribe(x=>{if(x==='SUBSCRIBED')setStatus(true,'Live sync')})}catch{}};document.head.appendChild(sc);setInterval(readLatest,3000)}function goTo(i){state.started=true;if(!state.startedAt)state.startedAt=Date.now();state.slide=Math.max(0,Math.min(i,SLIDES.length-1));state.black=false;clearModes();send()}function nextSlide(){if(state.overlay||state.activePoll||state.activeQuestion){clearTakeover();return}if(!state.started){goTo(0);return}if(state.slide<SLIDES.length-1)goTo(state.slide+1)}function prevSlide(){if(state.overlay||state.activePoll||state.activeQuestion){clearTakeover();return}if(!state.started)return;if(state.slide>0)goTo(state.slide-1)}function toggleBlack(){state.black=!state.black;if(state.black)clearModes();send()}function clearTakeover(){clearModes();state.black=false;send()}function pushVerse(i){const v=VERSES[i];if(!v)return;state.overlay={ref:v.ref,text:v.text};state.black=false;state.activePoll=null;state.activeQuestion=null;send()}function launchPoll(id){state.overlay=null;state.activeQuestion=null;state.black=false;state.activePoll={id,mode:'question'};send()}async function showResults(id){await loadAudienceData();const p=pollBy(id),c=voteCounts(p),tot=totalCounts(c);state.overlay=null;state.activeQuestion=null;state.black=false;state.activePoll={id,mode:'results',results:c,total:tot};send()}function closePoll(){state.activePoll=null;send()}function displayQuestion(i){const q=questions[i];if(!q)return;state.overlay=null;state.activePoll=null;state.black=false;state.activeQuestion={id:q.id||String(i),text:q.text};send()}function buildRemote(){document.getElementById('slide-list').innerHTML=SLIDES.map((s,i)=>`<button class="slide-item" onclick="goTo(${i})"><span class="si-n">${String(i+1).padStart(2,'0')}</span><span><span class="si-k">${esc(s.type)}</span><span class="si-t">${esc(titleOf(s))}</span></span></button>`).join('');document.getElementById('verse-list').innerHTML=VERSES.map((v,i)=>`<div class="verse-row"><div class="v-ref">${esc(v.ref)}</div><div class="v-text">${esc(v.text)}</div><button class="btn small" onclick="pushVerse(${i})">Push Everywhere</button></div>`).join('');try{const saved=JSON.parse(localStorage.getItem('kp4_alignment_remote')||'null');if(saved?.room===ROOM)state={...state,...saved,overlay:null,activePoll:null,activeQuestion:null,black:false}}catch{}setCue();loadAudienceData();setInterval(loadAudienceData,4500);setInterval(()=>{try{localStorage.setItem('kp4_alignment_remote',JSON.stringify({...state,overlay:null,activePoll:null,activeQuestion:null,black:false}))}catch{}},1200)}async function loadAudienceData(){if(!['remote','admin','mobile'].includes(view))return;try{const[q,p]=await Promise.all([fetch(`/api/questions-list?series_slug=${SERIES}&lesson_slug=${LESSON}`,{cache:'no-store'}),fetch(`/api/polls-list?series_slug=${SERIES}&lesson_slug=${LESSON}`,{cache:'no-store'})]),qd=await q.json().catch(()=>({})),pd=await p.json().catch(()=>({}));if(q.ok)questions=Array.isArray(qd.questions)?qd.questions:[];if(p.ok)votes=Array.isArray(pd.votes)?pd.votes:[];renderAudienceData()}catch{}}function renderAudienceData(){if(!['remote','admin','mobile'].includes(view))return;const qb=document.getElementById('q-badge'),ql=document.getElementById('q-list');if(qb)qb.textContent=questions.length;if(ql)ql.innerHTML=questions.length?questions.map((q,i)=>`<div class="qa-item"><div class="qa-text">${esc(q.text)}</div><div class="qa-meta">Anonymous</div><div class="qa-actions"><button class="btn small ${state.activeQuestion?.id===(q.id||String(i))?'on':'primary'}" onclick="${state.activeQuestion?.id===(q.id||String(i))?'clearTakeover()':`displayQuestion(${i})`}">${state.activeQuestion?.id===(q.id||String(i))?'Hide from Main':'Display on Main'}</button></div></div>`).join(''):'<div class="empty">No anonymous questions yet.</div>';const pb=document.getElementById('poll-badge'),pl=document.getElementById('poll-list');if(pb)pb.textContent=votes.length;if(pl)pl.innerHTML=POLLS.map(p=>{const c=voteCounts(p),tot=totalCounts(c);return`<div class="poll-admin"><div class="poll-cue">${esc(p.cue)}</div><div class="poll-q">${esc(p.question)}</div><div class="poll-meta">${tot} ${tot===1?'response':'responses'}</div><div class="poll-mini">${p.options.map(o=>{const n=c[o]||0,pct=tot?Math.round(n/tot*100):0;return`<div class="mini-row"><span>${esc(o)}</span><span class="mini-bar"><span style="width:${pct}%"></span></span><b>${n}</b></div>`}).join('')}</div><div class="qa-actions"><button class="btn small" onclick="launchPoll('${p.id}')">Launch</button><button class="btn small primary" onclick="showResults('${p.id}')">Results</button><button class="btn small" onclick="closePoll()">Close</button></div></div>`}).join('')}function mount(){if(view==='projector'){document.getElementById('projector').classList.remove('hidden');renderProjector()}else if(view==='confidence'){document.getElementById('confidence').classList.remove('hidden');renderConfidence();setInterval(updateTimer,500)}else if(view==='scriptures'){document.getElementById('scriptures').classList.remove('hidden');renderOutputs()}else if(view==='obsslides'){document.getElementById('obsslides').classList.remove('hidden');renderOutputs()}else if(view==='obslowerthirds'){document.body.style.background='transparent';document.getElementById('obslowerthirds').classList.remove('hidden');renderOutputs()}else{document.getElementById('remote').classList.remove('hidden');if(view==='mobile')document.body.classList.add('mobile');buildRemote()}startLocal();initNetwork()}Object.assign(window,{goTo,nextSlide,prevSlide,toggleBlack,clearTakeover,pushVerse,launchPoll,showResults,closePoll,displayQuestion,loadAudienceData});mount();})();